import { Mode } from '@/app/context/ModalContext';
import { getData } from '@/app/lib/getData';

interface CustomProps {
  mode: Mode;
}

export default async function ExerciseSecondStep({ mode }: CustomProps) {
  //   const workoutsList: Workout[] = await getData('workout');
  //   console.log(workoutsList);
  return <></>;
}
